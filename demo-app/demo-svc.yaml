apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  labels:
    app: demo-app

### Default labels for Prometheus Grafana stack ###
    #release: prometheus

  namespace: demo
spec:
  selector:
    app: demo-app

### Default labels for Prometheus Grafana stack ###
    #release: prometheus

  type: NodePort
  ports:
  - name: demo-app-port
    port: 8081
    targetPort: demo-app-port
