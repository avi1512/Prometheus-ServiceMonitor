apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo-smon
  labels:
    app: demo-app

### Default labels for Prometheus Grafana stack ###
    #release: prometheus

  namespace: demo
spec:
  endpoints:
  - port: demo-app-port
    path: /metrics
    interval: 15s
  namespaceSelector:
    matchNames:
    - demo
  selector:
    matchLabels:
      app: demo-app

### Default labels for Prometheus Grafana stack ###
      #release: prometheus
