apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  labels:
    release: prometheus
  namespace: demo
spec:
  selector:
    release: prometheus
  type: NodePort
  ports:
  - name: demo-app-port
    port: 8081
    targetPort: demo-app-port
