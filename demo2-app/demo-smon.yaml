apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
  name: demo2-smon
  namespace: demo2
spec:
  endpoints:
  - interval: 30s
    port: web
    path: /metrics
  selector:
    matchLabels:
      app: demo2-app
